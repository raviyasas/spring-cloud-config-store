server:
  port: 8090

hystrix:
  command:
    default:
      circuitBreaker:
        enabled: false
      execution:
        isolation:
          thread:
            timeoutInMillisecounds: 210000
        timeout:
          enabled: false
